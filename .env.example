# Backend
API_HOST=0.0.0.0
API_PORT=8000
DATABASE_URL=postgresql+psycopg://zappro:zappro@postgres:5432/zappro

# Frontend
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
FRONTEND_PORT=3000

# Postgres
POSTGRES_USER=zappro
POSTGRES_PASSWORD=change_me
POSTGRES_DB=zappro

# JWT (optional overrides — defaults to ephemeral dev keys if unset)
# ZAPPRO_JWT_PRIVATE_KEY_PATH=./keys/dev-jwt-private.pem
# ZAPPRO_JWT_PUBLIC_KEY_PATH=./keys/dev-jwt-public.pem

# Observação: segredos usados pelas pipelines (Slack webhook, SMTP, tokens de deploy) devem
# ser configurados como GitHub Actions Secrets:
#   - SLACK_WEBHOOK_URL
#   - CI_SMTP_SERVER / CI_SMTP_PORT / CI_SMTP_USERNAME / CI_SMTP_PASSWORD / CI_SMTP_FROM / CI_SMTP_RECIPIENTS
#   - STAGING/PRODUCTION tokens (SSH, API, etc.) conforme seu provedor
# Nunca versionar esses valores aqui.
